# Hermes Windows 部署工具 — 代码审查问题清单

审查日期: 2026-05-01 | 审查范围: 全部 34 个文件 | 问题总数: 94

---

## 致命 (6)

| # | 文件:行号 | 问题 |
|---|-----------|------|
| F1 | `desktop/chat/server/chat-endpoint.js:51` | 命令注入 — 用户消息直接拼入 spawn('hermes', ['chat', message]) |
| F2 | `desktop/chat/server/chat-endpoint.js:171` | 命令注入 — req.params.id 直接拼入 execSync |
| F3 | `scripts/build.ps1:37` | PowerShell 版本不兼容 — ?? 运算符仅 PS7+，PS5.1 默认崩溃 |
| F4 | `scripts/build.ps1:183` | 引用已删除的文件 — $installer 删除后又被调用 |
| F5 | `scripts/install-hermes.ps1:25` | CDN 明文 HTTP + 无校验 — 下载的脚本直接 root 执行 |
| F6 | `gui/.../InstallStep.xaml.cs:87` | 安装核心逻辑缺失 — RunInstallStep() 仅 Task.Delay(1200) 模拟 |

## 高 (11)

| # | 文件:行号 | 问题 |
|---|-----------|------|
| H1 | `desktop/chat/client/ChatPage.vue:90` | XSS — 自实现 Markdown v-html 无 script 过滤 |
| H2 | `desktop/hitl/server/approval-ws.js:全` | 无认证 — WebSocket 无 token 校验 |
| H3 | `gui/.../PowerShellRunner.cs:55` | 命令注入 — WSL 命令拼接未转义 |
| H4 | `scripts/wsl-bootstrap.sh:35` | 用户上下文错误 — git config --global 以当前用户执行 |
| H5 | `scripts/setup-mirrors.sh:25` | 硬编码 noble — 非 24.04 apt 源损坏 |
| H6 | `scripts/setup-systemd.sh:19` | 误判 systemd — pidof 不返回 PID1 |
| H7 | `scripts/integrate-desktop.sh:29` | /tmp 无验证读取 — 代码直接注入生产路径 |
| H8 | `scripts/install-hermes.ps1:13` | $MirrorGitee 参数未实现 |
| H9 | `scripts/install-hermes.ps1:233` | wsl.exe 不存在时调用 wsl 命令 |
| H10 | `scripts/install-hermes.ps1:258/325` | WSL 内核/rootfs 下载无 CDN 加速 |
| H11 | `scripts/install-hermes.ps1:639` | 仅检查最后一个 wsl 调用的退出码 |

## 中 (39)

| # | 文件 | 问题 |
|---|------|------|
| M1 | `install-hermes.ps1:410` | $existingUser.Trim() 空引用异常风险 |
| M2 | `install-hermes.ps1:404` | 密码通过命令行传递，ps 可见 |
| M3 | `install-hermes.ps1:677` | 健康检查始终返回 true，失败不可见 |
| M4 | `install-hermes.ps1:136` | Invoke-WithRetry 仅捕获终止错误 |
| M5 | `install-hermes.ps1:419` | Copy-Item 到 \\wsl$\ 无错误处理 |
| M6 | `post-install.ps1:52` | .wslconfig 硬编码与安装脚本冲突 |
| M7 | `post-install.ps1:11` | ErrorActionPreference="Stop" 过于激进 |
| M8 | `post-install.ps1:15/31/38/50/74` | 步骤序号混乱 |
| M9 | `post-install.ps1:89` | Read-Host 阻塞自动化 |
| M10 | `uninstall.ps1:45` | 防火墙规则仅删除第一个匹配项 |
| M11 | `uninstall.ps1:25` | wsl 命令退出码未检查 |
| M12 | `test-env.ps1:50` | 磁盘阈值 20GB vs 实际 5GB 不一致 |
| M13 | `test-env.ps1:78` | ipify.org 可能被墙 + 泄露公网 IP |
| M14 | `test-env.ps1:85` | 仅测试 GitHub，未测 CDN/镜像连通性 |
| M15 | `build.ps1:25` | 工具链下载无国内镜像 |
| M16 | `build.ps1:216` | 硬编码 win-x64 无 ARM64 |
| M17 | `build.ps1:60` | stderr 合并到 stdout 丢失诊断 |
| M18 | `wsl-bootstrap.sh:18/22` | apt-get 错误处理缺失 |
| M19 | `wsl-bootstrap.sh:39` | PATH 重复追加 |
| M20 | `setup-mirrors.sh:37-45` | 回滚后脚本继续，用户无感知 |
| M21 | `setup-mirrors.sh:49` | pip3 未安装时 set -e 风险 |
| M22 | `setup-systemd.sh:32` | su - 目标用户未登录时失败 |
| M23 | `setup-systemd.sh:48` | hermes-start 隐式依赖，无检查 |
| M24 | `integrate-desktop.sh:16` | npm 未装时 WEBUI_DIR 变字面量 |
| M25 | `integrate-desktop.sh:77` | sed 插入 import 无回滚 |
| M26 | `integrate-desktop.sh:109` | hermes gateway 后台失败不阻塞 |
| M27 | `integrate-desktop.sh:112` | sleep 3 竞态条件 |
| M28 | `ChatPage.vue:182` | keydown 监听未在 onUnmounted 移除 |
| M29 | `ChatPage.vue:118` | fetch 无 CSRF token |
| M30 | `ApprovalPanel.vue:96` | JSON.parse 无 try/catch |
| M31 | `ApprovalPanel.vue:129` | WebSocket 重连固定 3s，无退避 |
| M32 | `approval-ws.js:54` | Promise 存 Map 无内存清理 |
| M33 | `approval-ws.js:84` | 超时定时器未在服务关闭时清理 |
| M34 | `ToolHub.vue:257` | connect/disconnect 只切换状态无实际逻辑 |
| M35 | `ToolHub.vue:103` | URL 无校验，SSRF 风险 |
| M36 | `CheckStep.xaml.cs:86` | 脚本不存在返回"模拟通过" |
| M37 | `CheckStep.xaml.cs:93` | catch 吞异常无日志 |
| M38 | `PowerShellRunner.cs:15` | Verb="runas" 每次都弹 UAC |
| M39 | `PowerShellRunner.cs:55` | RunInWsl 未捕获 stderr |

## 低 (37)

| # | 文件 | 问题 |
|---|------|------|
| L1-L37 | 各处 | 空函数体、硬编码路径、`$env:SystemDrive` 替代 C:、引号拼接风险、Debug 构建不兼容、残留空目录、TOCTOU 竞态等 |
